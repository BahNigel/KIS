// app/config.jsx
const config = {
  fonts: {
    SpaceMono: require('../assets/fonts/SpaceMono-Regular.ttf'), // Add your font files here
  },
  theme: {
    dark: true, // Toggle between dark and light theme
  },
};

export default config;
